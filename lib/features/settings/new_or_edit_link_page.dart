import 'package:flutter/material.dart';

class NewOrEditLinkPage extends StatelessWidget {
  const NewOrEditLinkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}